<td class="mobile">
    {% if business_contact.locationLongitude|length >3 %}
        <a target="_blank"
           href="{{ path('show_location_google_maps',{id: business_contact.id}) }}"
           class="action" data-id="{{ business_contact.id }}"
           data-action="Map">
            <i title="Go to map" class='fas fa-map-marker-alt text-danger'></i></a>
    {% else %}
        {% if is_granted('ROLE_ADMIN') %}
            <button class="btn btn-primary btn-sm"
                    onclick="getLocation('{{ business_contact.id }}')">
                <i title="Set current location as location of the contact" class="fa fa-arrow-down"></i>
            </button>
        {% endif %}
    {% endif %}
</td>

<td class="desktop">
    {% if business_contact.locationLongitude|length >3 %}
        <a target="_blank"
           href="{{ path('show_location_google_maps',{id: business_contact.id}) }}"
           class="action" data-id="{{ business_contact.id }}"
           data-action="Map">
            <i title="Go to map" class='fas fa-map-marker-alt text-danger'></i></a>
    {% else %}
        {% if is_granted('ROLE_ADMIN') %}
            <button class="btn btn-primary btn-sm"
                    onclick="getLocation('{{ business_contact.id }}')">
                <i title="Set current location as location of the contact" class="fa fa-arrow-down"></i>
            </button>
        {% endif %}
    {% endif %}

    {{ business_contact.addressStreet }}{% if business_contact.addressStreet is not empty and business_contact.addressCity is not empty %}, {% endif %}{{ business_contact.addressCity }}{% if business_contact.addressCity is not empty and business_contact.addressPostCode is not empty %}, {% endif %}{{ business_contact.addressPostCode }}
</td>
