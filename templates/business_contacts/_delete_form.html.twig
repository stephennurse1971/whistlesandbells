<form method="post" action="{{ path('business_contacts_delete', {'id': business_contact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ business_contact.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
