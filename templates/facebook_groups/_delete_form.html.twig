<form method="post" action="{{ path('facebook_groups_delete', {'id': facebook_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facebook_group.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
