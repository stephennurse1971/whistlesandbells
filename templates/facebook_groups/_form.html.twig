{{ form_start(form) }}

<div class="row">
    <div class="col-2">
        {{ form_row(form.name) }}
    </div>
    <div class="col-2">
        {{ form_row(form.link) }}
    </div>
</div>
<div class="row">
    <div class="col-9">
        {{ form_row(form.comments) }}
    </div>
</div>

<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
