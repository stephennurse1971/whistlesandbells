{% extends 'base.html.twig' %}

{% block title %}Edit: Flight Price ({{ flight_stat.date|date('d-M-Y') }}){% endblock %}

{% block body %}
    <h1 style="color: red">Edit: Flight Price </h1>

    {{ include('flight_stats/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-secondary btn-sm" href="{{ path('flight_stats_index') }}">Return to list</a>

    {{ include('flight_stats/_delete_form.html.twig') }}
{% endblock %}
