<form method="post" action="{{ path('jpm_ic_history_delete', {'id': jpm_ic_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jpm_ic_history.id) }}">
    <button class="btn btn-danger btn-sm mt-2">Delete</button>
</form>
