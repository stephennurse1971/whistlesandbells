<form method="post" action="{{ path('company_details_delete', {'id': company_details.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_details.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
