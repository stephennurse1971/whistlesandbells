<form method="post" action="{{ path('bank_balances_delete', {'id': bank_balance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bank_balance.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
