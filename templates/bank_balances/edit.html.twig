{% extends 'base.html.twig' %}

{% block title %}Edit: Bank Balance{% endblock %}

{% block body %}
    <h1 style="color: red">Edit: Bank Balance</h1>

    {{ include('bank_balances/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('bank_balances_index') }}">Return to list</a>

    {{ include('bank_balances/_delete_form.html.twig') }}
{% endblock %}
