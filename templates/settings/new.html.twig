{% extends 'base.html.twig' %}

{% block title %}New Settings{% endblock %}

{% block body %}
    <h1 style="color: red">New Settings</h1>

    {{ include('settings/_form.html.twig') }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('settings_index') }}">Return to list</a>
{% endblock %}
