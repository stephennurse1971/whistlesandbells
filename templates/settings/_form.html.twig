{{ form_start(form) }}

<div class="row">
    <div class="col-md-3">
        <h2 style="color: blue">Flight scraper</h2>
        {{ form_row(form.flightStatsDays) }}
        {{ form_row(form.flightStatsStartDate) }}
    </div>

    <div class="col-md-3">
        <h2 style="color: blue">Investment Analysis</h2>
        {{ form_row(form.investmentDate) }}
    </div>
</div>

<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
