{% extends 'base.html.twig' %}

{% block title %}Edit Settings{% endblock %}

{% block body %}
    <h1 style="color: red">Edit Settings</h1>

    {{ include('settings/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('settings_index') }}">Return to list</a>

    {{ include('settings/_delete_form.html.twig') }}
{% endblock %}
