<form method="post" action="{{ path('settings_delete', {'id': setting.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ setting.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
