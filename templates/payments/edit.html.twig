{% extends 'base.html.twig' %}

{% block title %}Edit: Payments{% endblock %}

{% block body %}
    <h1 style="color: red;">Edit Payments</h1>

    {{ include('payments/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm  mt-2" href="{{ path('payments_index') }}">Return to list</a>

    {{ include('payments/_delete_form.html.twig') }}
{% endblock %}
