{% extends 'base.html.twig' %}

{% block title %}Edit: {{ dog_skill.description}}{% endblock %}

{% block body %}
    <h1 style="color: red">Edit: {{ dog_skill.description}}</h1>

    {{ include('dog_skills/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('dog_skills_index') }}">Return to list</a>

    {{ include('dog_skills/_delete_form.html.twig') }}
{% endblock %}

{% block additionaljs %}
     <script>
        ClassicEditor
            .create(document.querySelector('#dog_skills_notes'))
            .catch(error => {
                console.error(error);
            });
    </script>

{% endblock %}
