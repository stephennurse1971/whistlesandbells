<form method="post" action="{{ path('market_data_delete', {'id': market_data.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ market_data.id) }}">
    <button class="btn btn-danger btn-sm mt-2">Delete</button>
</form>
