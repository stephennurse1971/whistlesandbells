<form method="post" action="{{ path('tennis_bookings_delete', {'id': tennis_booking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tennis_booking.id) }}">
    <button class="btn btn-danger btn-sm mt-2">Delete</button>
</form>
