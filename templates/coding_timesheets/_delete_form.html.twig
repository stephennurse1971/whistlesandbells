<form method="post" action="{{ path('coding_timesheets_delete', {'id': coding_timesheet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coding_timesheet.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
