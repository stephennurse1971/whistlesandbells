{% extends 'base.html.twig' %}

{% block title %}  {{ cms_copy_page_format.name }}{% endblock %}

{% block body %}
    <h1 style="color: red">CMS Page Layout Format: {{ cms_copy_page_format.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cms_copy_page_format.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cms_copy_page_format.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cms_copy_page_format.description }}</td>
            </tr>
            <tr>
                <th>Pros</th>
                <td>{{ cms_copy_page_format.pros }}</td>
            </tr>
            <tr>
                <th>Cons</th>
                <td>{{ cms_copy_page_format.cons }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ cms_copy_page_format.code }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm" href="{{ path('cms_copy_page_formats_index') }}">Return to list</a>

    <a class="btn btn-outline-danger btn-sm" href="{{ path('cms_copy_page_formats_edit', {'id': cms_copy_page_format.id}) }}">Edit</a>

    {{ include('CmsCopyPageFormats/_delete_form.html.twig') }}
{% endblock %}
