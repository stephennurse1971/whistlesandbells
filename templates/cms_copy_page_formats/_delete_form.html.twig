<form method="post" action="{{ path('cms_copy_page_formats_delete', {'id': cms_copy_page_format.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cms_copy_page_format.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
