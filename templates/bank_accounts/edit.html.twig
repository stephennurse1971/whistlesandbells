{% extends '../../base.html.twig' %}

{% block title %}Edit: Bank Accounts{% endblock %}

{% block body %}
    <h1 style="color: red">Edit: Bank Accounts</h1>

    {{ include('bank_accounts/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('bank_accounts_index') }}">Return to list</a>

    {{ include('bank_accounts/_delete_form.html.twig') }}
{% endblock %}
