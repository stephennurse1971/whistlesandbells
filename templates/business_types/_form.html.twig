<style>
    .ck-content {
        height: 500px;
    }
</style>
{{ form_start(form) }}
<div class="row">
    <div class="col-2">
        {{ form_row(form.businessType) }}
        {{ form_row(form.mapIcon) }}
        {{ form_row(form.mapIconColour) }}
        {{ form_row(form.mapDisplay) }}
    </div>
</div>

<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}




