{% extends 'base.html.twig' %}

{% block title %}BusinessTypes{% endblock %}

{% block body %}
    <h1 style="color: red">BusinessTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ business_type.id }}</td>
            </tr>
            <tr>
                <th>BusinessType</th>
                <td>{{ business_type.businessType }}</td>
            </tr>
            <tr>
                <th>BusinesCategory</th>
                <td>{{ business_type.businesCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm" href="{{ path('business_types_index') }}">Return to list</a>

    <a class="btn btn-outline-danger btn-sm" href="{{ path('business_types_edit', {'id': business_type.id}) }}">Edit</a>

    {{ include('business_types/_delete_form.html.twig') }}
{% endblock %}
