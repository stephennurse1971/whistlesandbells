<form method="post" action="{{ path('to_do_list_delete', {'id': to_do_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ to_do_list.id) }}">
    <button class="btn btn-danger btn-sm mt-2">Delete</button>
</form>
