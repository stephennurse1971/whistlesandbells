{% extends 'base.html.twig' %}

{% block title %}CV{% endblock %}

{% block body %}
    <h1 style="color: red">CV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ curriculum_vitae.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ curriculum_vitae.date ? curriculum_vitae.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>JobDescription</th>
                <td>{{ curriculum_vitae.jobDescription }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm" href="{{ path('curriculum_vitae_index') }}">Return to list</a>

    <a class="btn btn-outline-danger btn-sm " href="{{ path('curriculum_vitae_edit', {'id': curriculum_vitae.id}) }}">Edit</a>

    {{ include('curriculum_vitae/_delete_form.html.twig') }}
{% endblock %}
