{{ form_start(form) }}
<div class="row">
    <div class="col-4">
        {{ form_row(form.fx) }}
        {{ form_row(form.currentFxRate) }}
    </div>
</div>
    <button class="btn btn-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
