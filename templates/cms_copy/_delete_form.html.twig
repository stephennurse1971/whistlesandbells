<form method="post" action="{{ path('cms_copy_delete', {'id': cms_copy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cms_copy.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
