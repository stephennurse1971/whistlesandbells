{% extends '../../base.html.twig' %}

{% block title %}Prospect Employer{% endblock %}

{% block body %}
    <h1 style="color: red">Prospect Employer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prospect_employer.id }}</td>
            </tr>
            <tr>
                <th>Employer</th>
                <td>{{ prospect_employer.employer }}</td>
            </tr>
            <tr>
                <th>InterviewDate1</th>
                <td>{{ prospect_employer.interviewDate1 ? prospect_employer.interviewDate1|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>InterviewDate2</th>
                <td>{{ prospect_employer.interviewDate2 ? prospect_employer.interviewDate2|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>InterviewDate3</th>
                <td>{{ prospect_employer.interviewDate3 ? prospect_employer.interviewDate3|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm" href="{{ path('prospect_employer_index') }}">Return to list</a>

    <a class="btn btn-outline-danger btn-sm" href="{{ path('prospect_employer_edit', {'id': prospect_employer.id}) }}">Edit</a>

    {{ include('prospect_employer/_delete_form.html.twig') }}
{% endblock %}
