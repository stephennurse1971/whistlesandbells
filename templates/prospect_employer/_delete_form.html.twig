<form method="post" action="{{ path('prospect_employer_delete', {'id': prospect_employer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prospect_employer.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
