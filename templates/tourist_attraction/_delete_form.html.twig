<form method="post" action="{{ path('tourist_attraction_delete', {'id': tourist_attraction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tourist_attraction.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
