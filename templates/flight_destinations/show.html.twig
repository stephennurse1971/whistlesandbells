{% extends 'base.html.twig' %}

{% block title %}Flight Destinations{% endblock %}

{% block body %}
    <h1 style="color: red">Flight Destinations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flight_destination.id }}</td>
            </tr>
            <tr>
                <th>DepartureCity</th>
                <td>{{ flight_destination.departureCity }}</td>
            </tr>
            <tr>
                <th>DepartureCode</th>
                <td>{{ flight_destination.departureCode }}</td>
            </tr>
            <tr>
                <th>ArrivalCity</th>
                <td>{{ flight_destination.arrivalCity }}</td>
            </tr>
            <tr>
                <th>ArrivalCode</th>
                <td>{{ flight_destination.arrivalCode }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm" href="{{ path('flight_destinations_index') }}">Return to list</a>

    <a class="btn btn-outline-danger btn-sm"  href="{{ path('flight_destinations_edit', {'id': flight_destination.id}) }}">Edit</a>

    {{ include('flight_destinations/_delete_form.html.twig') }}
{% endblock %}
