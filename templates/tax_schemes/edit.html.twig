{% extends 'base.html.twig' %}

{% block title %}Edit: Tax Schemes ({{ tax_scheme.name }}){% endblock %}

{% block body %}
    <h1 style="color: red">Edit Tax Schemes</h1>

    {{ include('tax_schemes/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('tax_schemes_index') }}">Return to list</a>

    {{ include('tax_schemes/_delete_form.html.twig') }}
{% endblock %}
