{% extends '../../base.html.twig' %}

{% block title %}File Attachments{% endblock %}

{% block body %}
    <h1 style="color: red">File Attachments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ file_attachment.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ file_attachment.date ? file_attachment.date|date('Y-m-d') : '' }}</td>
            </tr>


        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm mt-2" href="{{ path('file_attachment_index') }}">Return to list</a>

    <a class="btn btn-outline-success btn-sm mt-2" href="{{ path('file_attachments_edit', {'id': file_attachment.id}) }}">Edit</a>

    {{ include('file_attachments/_delete_form.html.twig') }}
{% endblock %}
