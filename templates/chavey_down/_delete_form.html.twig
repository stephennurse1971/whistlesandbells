<form method="post" action="{{ path('chavey_down_delete', {'id': chavey_down.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chavey_down.id) }}">
    <button class="btn btn-danger mt-2">Delete</button>
</form>
