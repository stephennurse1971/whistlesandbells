{% extends 'base.html.twig' %}

{% block title %}Chavey Down{% endblock %}

{% block body %}
    <h1 style="color: red">Chavey Down</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chavey_down.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ chavey_down.date ? chavey_down.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Vendor</th>
                <td>{{ chavey_down.vendor }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ chavey_down.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm mt-2" href="{{ path('chavey_down_index') }}">back to list</a>

    <a class="btn btn-outline-success btn-sm mt-2" href="{{ path('chavey_down_edit', {'id': chavey_down.id}) }}">Edit</a>

    {{ include('chavey_down/_delete_form.html.twig') }}
{% endblock %}
