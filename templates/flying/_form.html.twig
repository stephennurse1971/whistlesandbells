{{ form_start(form) }}
<div class="row">
    <div class="col-2">
        {{ form_row(form.date) }}
        {{ form_row(form.instructor) }}
        {{ form_row(form.aircraft) }}
        {{ form_row(form.hots) }}
        {{ form_row(form.price) }}
        {{ form_row(form.paymentMade) }}
    </div>
    <div class="col-8">
        {{ form_row(form.description) }}
        {{ form_row(form.lessonsLearnt) }}
    </div>
</div>

<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
