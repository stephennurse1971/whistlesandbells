{% extends '../../base.html.twig' %}

{% block title %}IntroductionSegment{% endblock %}

{% block body %}
    <h1 style="color: red">Introduction Segment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ introduction_segment.id }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ introduction_segment.country }}</td>
            </tr>
            <tr>
                <th>EmailSegment</th>
                <td>{{ introduction_segment.emailSegment }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-info btn-sm" href="{{ path('introduction_segment_index') }}">Return to list</a>

    <a class="btn btn-outline-danger btn-sm" href="{{ path('introduction_segment_edit', {'id': introduction_segment.id}) }}">Edit</a>

    {{ include('introduction_segment/_delete_form.html.twig') }}
{% endblock %}
