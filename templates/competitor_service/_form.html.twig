{{ form_start(form) }}

<div class="row">
    <div class="col-2">
        {{ form_row(form.competitor) }}
        {{ form_row(form.service) }}
        {{ form_row(form.price) }}
    </div>

    <div class="col-1"></div>

    <div class="col-8">
        {{ form_row(form.description) }}
    </div>


</div>

<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


