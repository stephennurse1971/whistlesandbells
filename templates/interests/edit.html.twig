{% extends 'base.html.twig' %}

{% block title %}Page SetUp: {{ interest.name }}{% endblock %}

{% block body %}
    <h1 style="color: red">Page SetUp: {{ interest.name }}</h1>

    {{ include('interests/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary btn-sm" href="{{ path('interests_index') }}">Return to list</a>

    {{ include('interests/_delete_form.html.twig') }}
{% endblock %}
