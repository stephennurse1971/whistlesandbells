<form method="post" action="{{ path('investment_future_comms_delete', {'id': investment_future_comm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ investment_future_comm.id) }}">
    <button class="btn btn-danger btn-sm mt-2">Delete</button>
</form>
