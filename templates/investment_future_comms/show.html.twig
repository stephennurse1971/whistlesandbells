{% extends '../../base.html.twig' %}

{% block title %}InvestmentFutureComms{% endblock %}

{% block body %}
    <h1>Investment FutureComms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ investment_future_comm.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ investment_future_comm.date ? investment_future_comm.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Attachment</th>
                <td>{{ investment_future_comm.attachment }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ investment_future_comm.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary btn-sm mt-2" href="{{ path('investment_future_comms_index') }}">Return to list</a>

    <a class="btn btn-outline-info btn-sm mt-2" href="{{ path('investment_future_comms_edit', {'id': investment_future_comm.id}) }}">Edit</a>

    {{ include('investment_future_comms/_delete_form.html.twig') }}
{% endblock %}
