{{ form_start(form) }}
<div class="row">
    <div class="col-md-3">
        <h2>Investment Documents</h2>

            {{ form_row(form.marketData ) }}
            {{ form_row(form.date) }}
            {{ form_row(form.comment) }}
            {{ form_row(form.attachment) }}


    </div>
</div>
<button class="btn btn-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
