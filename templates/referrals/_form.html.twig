{{ form_start(form) }}
<div class="row">
    <div class="col-md-3">
        <h2>Photo</h2>
        {{ form_row(form.user) }}
        {{ form_row(form.businessSite) }}
        {{ form_row(form.dateTime) }}
        {{ form_row(form.action) }}
    </div>
</div>
<br>
<br>
<button class="btn btn-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
