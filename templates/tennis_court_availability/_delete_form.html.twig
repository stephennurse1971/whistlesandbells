<form method="post" action="{{ path('tennis_court_availability_delete', {'id': tennis_availability.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tennis_availability.id) }}">
    <button class="btn btn-outline-danger btn-sm mt-2">Delete</button>
</form>
