{% extends 'base.html.twig' %}

{% block title %}TennisAvailability{% endblock %}

{% block body %}
    <h1 style="color: red">TennisAvailability</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tennis_availability.id }}</td>
            </tr>
            <tr>
                <th>Venue</th>
                <td>{{ tennis_availability.venue }}</td>
            </tr>
            <tr>
                <th>Hour</th>
                <td>{{ tennis_availability.hour }}</td>
            </tr>
            <tr>
                <th>Available</th>
                <td>{{ tennis_availability.available }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-success bt-sm mt-2" href="{{ path('tennis_court_availability_index') }}">Return to list</a>

    <a class="btn btn-outline-danger bt-sm mt-2" href="{{ path('tennis_court_availability_edit', {'id': tennis_availability.id}) }}">edit</a>

    {{ include('tennis_court_availability/_delete_form.html.twig') }}
{% endblock %}
