<form method="post" action="{{ path('default_tennis_player_availability_hours_delete', {'id': default_tennis_player_availability_hour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ default_tennis_player_availability_hour.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
