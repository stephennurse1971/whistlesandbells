<form method="post" action="{{ path('app_fx_rates_history_delete', {'id': fx_rates_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fx_rates_history.id) }}">
    <button class="btn btn">Delete</button>
</form>
