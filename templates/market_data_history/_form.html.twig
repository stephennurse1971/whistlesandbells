{{ form_start(form) }}
<div class="row">
    <div class="col-2">
        {{ form_row(form.date) }}
        {{ form_row(form.security) }}
        {{ form_row(form.marketPrice) }}
    </div>

</div>
<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
