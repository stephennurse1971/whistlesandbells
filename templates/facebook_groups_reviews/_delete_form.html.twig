<form method="post" action="{{ path('facebook_groups_reviews_delete', {'id': facebook_groups_review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facebook_groups_review.id) }}">
    <button class="btn btn-outline-danger btn-sm">Delete</button>
</form>
