{{ form_start(form) }}

<div class="row">
    <div class="col-2">
        {{ form_row(form.facebookGroup) }}
        {{ form_row(form.date) }}
        {{ form_row(form.reviewer) }}
        {{ form_row(form.comment) }}
    </div>
    <div class="col-2">
    </div>
</div>
    <button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
