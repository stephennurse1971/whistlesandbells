<form method="post" action="{{ path('to_do_list_items_delete', {'id': to_do_list_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ to_do_list_item.id) }}">
    <button class="btn btn-outline-danger btn-sm">Delete</button>
</form>
