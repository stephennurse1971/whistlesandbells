{% extends 'base.html.twig' %}

{% block title %}Edit Country{% endblock %}

{% block body %}
    <h1 style="color: red">Edit Country</h1>

    {{ include('country/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-secondary btn-sm mt-2" href="{{ path('country_index') }}">Return to list</a>

    {{ include('country/_delete_form.html.twig') }}
{% endblock %}
