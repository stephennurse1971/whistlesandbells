{{ form_start(form) }}
<div class="row">
    <div class="col-3">
        {{ form_widget(form) }}
    </div>
</div>
<button class="btn btn-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
