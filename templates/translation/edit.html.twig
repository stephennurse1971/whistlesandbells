{% extends 'base.html.twig' %}

{% block title %}Edit: Translation{% endblock %}

{% block body %}
    <h1 style="color: red">Edit: Translation</h1>

    {{ include('translation/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary" href="{{ path('translation_index') }}">Return to list</a>

    {{ include('translation/_delete_form.html.twig') }}
{% endblock %}
