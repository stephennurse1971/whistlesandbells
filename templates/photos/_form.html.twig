{{ form_start(form) }}
<div class="row">
    <div class="col-md-3">
        {{ form_row(form.location) }}
        {{ form_row(form.uploadedBy) }}
    </div>
    <div class="col-md-8">
        {{ form_row(form.photoFile) }}
    </div>
</div>
<div class="row">
    <div class="col-md-3">
        {{ form_row(form.favourites) }}
    </div>
</div>
<hr>
<button class="btn btn-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
