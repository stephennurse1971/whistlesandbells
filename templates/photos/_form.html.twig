{{ form_start(form) }}
<div class="row">
    <div class="col-md-3">
        <h2>Photo</h2>
        {{ form_row(form.location) }}
        {{ form_row(form.photos) }}
        {{ form_row(form.uploadedBy) }}
        {{ form_row(form.date) }}
{#        {{ form_row(form.highPriority) }}#}
    </div>
</div>
<br>
<br>
<br>
<br>
    <button class="btn btn-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
