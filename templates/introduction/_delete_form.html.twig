<form method="post" action="{{ path('introduction_delete', {'id': introduction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ introduction.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
