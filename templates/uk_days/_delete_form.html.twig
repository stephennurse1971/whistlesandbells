<form method="post" action="{{ path('uk_days_delete', {'id': uk_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uk_day.id) }}">
    <button class="btn btn-outline-danger btn-sm">Delete</button>
</form>
