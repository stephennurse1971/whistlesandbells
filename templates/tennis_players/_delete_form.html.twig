<form method="post" action="{{ path('tennis_players_delete', {'id': tennis_player.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tennis_player.id) }}">
    <button class="btn btn-outline-danger btn-sm mt-2">Delete</button>
</form>
