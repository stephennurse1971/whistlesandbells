{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">
        <h2> </h2>
        {{ form_row(form.taxYear) }}
        {{ form_row(form.date) }}
        {{ form_row(form.comments) }}
        {{ form_row(form.detailedComments) }}
        {{ form_row(form.attachment) }}
    </div>

</div>
    <button class="btn btn-outline-success btn-sm mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
